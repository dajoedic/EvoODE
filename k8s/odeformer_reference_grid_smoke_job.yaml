apiVersion: batch/v1
kind: Job
metadata:
  name: evoode-odeformer-reference-grid-smoke
  namespace: scch-das
  labels:
    app.kubernetes.io/name: evoode
    app.kubernetes.io/part-of: evoode-phase-c
    app.kubernetes.io/component: odeformer-reference-grid-smoke
    app.kubernetes.io/managed-by: kubectl
    hpc.scch.at/service: evoode-phase-c-cells
    hpc.scch.at/responsibility: joedicke
spec:
  # Run before the 126-completion grid to catch image-pull, NFS, ODEFormer
  # weight, and trajectory-export failures cheaply.
  ttlSecondsAfterFinished: 3600
  backoffLimit: 2
  activeDeadlineSeconds: 3600
  template:
    metadata:
      labels:
        app.kubernetes.io/name: evoode
        app.kubernetes.io/part-of: evoode-phase-c
        app.kubernetes.io/component: odeformer-reference-grid-smoke
        hpc.scch.at/service: evoode-phase-c-cells
        hpc.scch.at/responsibility: joedicke
    spec:
      restartPolicy: Never
      imagePullSecrets:
        - name: evoode-gitlab-pull
      containers:
        - name: cell
          image: registry.gitlab.scch.at:443/joedicke/evoode/odeformer-reference:<COMMIT_SHA>
          imagePullPolicy: IfNotPresent
          command:
            - python
            - -m
            - baselines.run_odeformer_grid_k8s
          args:
            - --config
            - /workspace/EvoODE/baselines/configs/odeformer_grid.json
            - --trajectory-export-dir
            - /outputs/odeformer_grid_<COMMIT_SHA>/trajectory_export
            - --output-dir
            - /outputs/odeformer_grid_<COMMIT_SHA>/reference/smoke
            - --limit
            - "2"
          env:
            - name: JOB_COMPLETION_INDEX
              value: "0"
            - name: OMP_NUM_THREADS
              value: "1"
            - name: MKL_NUM_THREADS
              value: "1"
            - name: OPENBLAS_NUM_THREADS
              value: "1"
            - name: ODEFORMER_TORCH_THREADS
              value: "1"
          resources:
            requests:
              cpu: "1"
              # Estimated; WP-N23 recorded runtime but no memory-watermark column.
              memory: 4Gi
            limits:
              cpu: "1"
              memory: 4Gi
          volumeMounts:
            - name: evoode-bigdata
              mountPath: /outputs
              subPath: data-science/joedicke
      volumes:
        - name: evoode-bigdata
          nfs:
            server: nfs.orion.scch.at
            path: /bigdata
