Kein aktiver Task.

Zuletzt abgeschlossen: WP-R1 (suchfreie Referenz ueber die volle Basis).
Codex meldete `blocked` — Julia startet in seiner Sandbox nicht. Die Abnahme hat Claude gefahren
und dabei zwei Fehler im nie ausgefuehrten Skript behoben: fehlendes `include` und zwei
unqualifizierte Aufrufe nicht exportierter Funktionen. Report `docs/WP-R1.md`.
